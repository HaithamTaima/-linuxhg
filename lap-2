#include <fcntl.h>
#include <unistd.h>

int main(int argc, char *argv[]) {
    int fd;

    fd = open("output.txt", O_CREAT | O_TRUNC | O_RDWR, 0777);

    dup2(fd, 1); // redirect stdout
    close(fd);

    execlp("ls", "ls", NULL);

    return 0;
}
